DROP TABLE IF EXISTS development_tmp;
CREATE TABLE development_tmp(
    GRC text,
    GRC2 text,
    bbl text,
    bcode text,
    bin text,
    cblock text,
    cd text,
    council text,
    csd text,
    ctract text,
    hnum text,
    lat text,
    lon text,
    msg text, 
    nta text,
    ntan text,
    sname text,
    uid text,
    zipcode text
);